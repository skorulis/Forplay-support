This project contains classes used in multiple projects when creating forplay games.

Currently the most useful of these are in the UI project which makes it easy to create a UI for a forplay game.

Expect the classes in this library to change without warning.